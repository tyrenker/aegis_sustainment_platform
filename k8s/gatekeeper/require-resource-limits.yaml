# TODO (Phase 3, step 4): second policy — deny any pod without resource limits set.
# Same ConstraintTemplate + Constraint shape as no-privileged-pods.yaml — write this one from
# scratch too rather than copying, so the Rego syntax actually sinks in the second time.
